# strands-pack environment template
# Copy to ".env" (or export in your shell) and fill in values for the tools you use.
# Never commit real secrets.

# --- Discord (bot REST API) ---
DISCORD_BOT_TOKEN=
# Optional defaults so you can omit IDs in prompts:
DISCORD_GUILD_ID=
DISCORD_CHANNEL_ID=

# --- Google AI (Gemini tools: gemini_image/gemini_video/gemini_music + gpt_image provider=gemini) ---
GOOGLE_API_KEY=
# Optional alias (if you already use GEMINI_API_KEY elsewhere)
GEMINI_API_KEY=

# --- Local storage defaults (SQLite / ChromaDB) ---
# If set, the sqlite tool will use this path when db_path is omitted.
SQLITE_DB_PATH=./data/app.db
# If set, the chromadb_tool will use this directory when persist_directory is omitted.
CHROMA_PERSIST_DIRECTORY=./chroma_data

# --- Google Workspace (Gmail / Docs / Calendar / Forms / Drive / Sheets / Tasks) ---
# These tools typically use Google Auth (ADC / service account / OAuth authorized-user token files).
#
# Option A (common for servers): set a service account key JSON path for Application Default Credentials.
# google-auth will pick this up automatically for auth_type="application_default".
GOOGLE_APPLICATION_CREDENTIALS=
#
# Option B (common for local dev): point to an "authorized user" token JSON file (OAuth installed-app flow).
# Many tools accept this as `authorized_user_file=...` (and Gmail as `token_path=...`).
GOOGLE_AUTHORIZED_USER_FILE=
#
# OAuth client secrets JSON (Desktop app) used to generate authorized-user tokens (google_auth tool / examples script).
GOOGLE_CLIENT_SECRETS_FILE=
#
# Optional (Workspace admin): user to impersonate with domain-wide delegation when using a service account.
GOOGLE_DELEGATED_USER=

# --- AWS ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
# Optional: used by some invoke-permission source ARNs (API Gateway/Scheduler)
AWS_ACCOUNT_ID=
# Optional: extra tags to apply to AWS resources created by strands-pack tools.
# Format: JSON object or "k=v,k2=v2"
STRANDS_PACK_AWS_TAGS={"env":"dev","owner":"you"}
#
# Lambda safety/guardrails
STRANDS_PACK_LAMBDA_PREFIX=agent-
# Production mode: only allow these execution role ARNs (comma-separated). When set, auto role creation is disabled.
STRANDS_PACK_LAMBDA_ROLE_ALLOWLIST=
# Dev mode convenience: allow auto-creation of a logs-only execution role when role_arn is omitted.
STRANDS_PACK_LAMBDA_ALLOW_AUTO_ROLE_CREATE=false
#
# API Gateway / Scheduler naming guards
STRANDS_PACK_API_PREFIX=agent-
STRANDS_PACK_SCHEDULE_PREFIX=agent-

# --- Notifications (notify tool) ---
# Local-only sound notifications. For cloud notifications, use the `sns` tool (or a webhook tool).

# --- GitHub ---
GITHUB_TOKEN=

# --- OpenAI (gpt_image provider=openai + embeddings) ---
OPENAI_API_KEY=

# --- YouTube ---
# For public data access (search, get_videos, get_channels, list_playlists, list_playlist_items),
# you can use an API key:
YOUTUBE_API_KEY=
# Optional defaults so you can omit IDs in prompts:
YOUTUBE_CHANNEL_ID=
YOUTUBE_UPLOADS_PLAYLIST_ID=
# Legacy aliases (still supported):
MY_CHANNEL_ID=
MY_UPLOADED_VIDEO_PLAYLIST_ID=

# --- Notion ---
NOTION_TOKEN=

# --- Calendly ---
CALENDLY_TOKEN=

# --- LinkedIn ---
LINKEDIN_ACCESS_TOKEN=

# --- X (Twitter) ---
X_BEARER_TOKEN=

# --- Twilio ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# --- Philips Hue ---
HUE_BRIDGE_IP=

# (End of file)
